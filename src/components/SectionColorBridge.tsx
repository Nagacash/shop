/** Solid gradient band between light hero and dark video sections — no overlap. */
export default function SectionColorBridge() {
  return <div className="section-color-bridge" aria-hidden="true" />;
}
